// MLXBerniniR — MLXEngine wrapper over the BerniniR core (S7 in PORTING-SPEC.md).
// Placeholder until the core reaches t2v parity (S2); the conformant
// `BerniniRPackage: ModelPackage` (textToVideo + textToImage) lands here.

import BerniniR
import MLXToolKit
